# Compose Deployment

Docker Compose files will be added in TASK-009.

Local development PostgreSQL is available at the repository root in `compose.dev.yml`.
See `docs/local-development.md` for the current development workflow.

Target services:

- `caddy`
- `web`
- `api`
- `mcp`
- `postgres`
